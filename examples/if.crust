include "stdio";

fn func(a: int) -> int {
    if a {
        return 0;
    };

    return 1;
};

fn main() -> int {
    let a: int = 0;
    let b: int = 1;
    if b {
        if func(b) {
            print("kekw\n");
        };

        print("test\n");
    };

    return 0;
};
