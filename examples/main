let x: str = "glb\n";

fn other(param: str) -> str {
    return param;
};

fn test(a: str) -> str {
    return other(a);
};

fn main() -> int {
    let teststr: str = "a\n";
    let a: str = test(teststr);
    pront(a);
    pront(x);
    return 0;
};

