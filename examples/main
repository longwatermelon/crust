fn test() -> int {
    let x: int = 5;
    let y: str = "string test\n";
    let z: str = y;
    let a: int = x;

    pront(y, z, y, "sample text\n");
    return 9;
};

fn main() -> int {
    let x: str = "string\n";
    pront(x);
    test();
    return 0;
};

