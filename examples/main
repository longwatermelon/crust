let x: str = "glb\n";

fn test(a: str, b: str, c: str) -> str {
    pront(b, c);
    return a;
};

fn main() -> int {
    let a: str = test("test\n", "test", "test");
    pront(a);
    pront(x);
    return 0;
};

