struct example {
    a: int,
    b: str
};

fn func(a: str, b: str, c: int) -> str {
    pront(a, b, "    \n");
    return b;
};


fn main() -> int {
    func("aaaaa", "bbbbb", 0);

    pront("   \n\n");

    let a: str = "aaaaa";
    let b: str = "bbbbb";
    let c: int = 9;

    func(a, "bbbbb", 0);
    func(b, "bbbbb", 0);

    let b2: str = b;
    pront(b2, "    \n");
    b = a;
    b2 = func(a, b, 0);

    pront(b2);

    pront("    \n");
    return 0;
};

