fn test(a: str, b:str, c: str) -> str {
    pront(a, b, c);
    return "string";
};

fn main() -> int {
    let b: str = "lool\n";
    let c: str = b;
    let a: str = "str\n";
    test("test\n", c, a);
    return 0;
};

