fn test(b: str) -> str {
    let a: str = "strr\n";
    pront(a);
    return b;
};

fn empty() -> void {};

fn main() -> int {
    let b: str = test("test\n");
    pront(b);
    let a: str = b;

    pront("    \n");
    let c: str = test(a);
    pront(c);

    empty();

    return 0;
};
